from Distance import Distance
import math

# TODO fazer a distancia coseno provavelmente usando numpy


class CosineDistance(Distance):

    def calculate(self, vector: list, points: list) -> float:
        return super().calculate(vector, points)
